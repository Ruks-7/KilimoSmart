name: Run Tests

# Trigger the workflow on push and pull requests
on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    branches: [ master, main, develop ]

# Set environment variables
env:
  NODE_ENV: test

jobs:
  test:
    name: Test Suite
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [16.x, 18.x]
    
    steps:
      # Checkout the code
      - name: Checkout code
        uses: actions/checkout@v3
      
      # Setup Node.js
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      
      # Install backend dependencies
      - name: Install backend dependencies
        run: |
          cd backend
          npm ci
      
      # Install jest-junit temporarily to generate JUnit XML report
      - name: Install jest-junit (CI only)
        run: |
          cd backend
          npm install --no-save jest-junit@^12

      # Run all tests with JUnit reporter so test-reporter can find results
      - name: Run tests (generate junit)
        env:
          JEST_JUNIT_OUTPUT: backend/coverage/junit.xml
        run: |
          cd backend
          mkdir -p coverage
          npx jest --ci --reporters=default --reporters=jest-junit --coverage --coverageDirectory=coverage
      
      # Upload coverage reports
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          directory: ./backend/coverage
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false
      
      # Report test results
      - name: Test Report
        uses: dorny/test-reporter@v1
        if: success() || failure()
        with:
          name: Jest Tests
          path: backend/coverage/junit.xml
          reporter: jest-junit
          fail-on-error: false
